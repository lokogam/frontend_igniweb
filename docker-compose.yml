version: '3.8'

services:
  frontend:
    build:
      context: ./
      dockerfile: Dockerfile
    container_name: frontend_igniweb
    restart: always
    ports:
      - 8000:8000
    # volumes:
      # - ./frontend:/usr/src/app
